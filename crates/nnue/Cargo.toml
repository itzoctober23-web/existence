[package]
name = "nnue"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Evaluation network skeleton. The architecture is data; the weights are learned."

[dependencies]
board = { path = "../board" }
