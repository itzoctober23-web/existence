[package]
name = "grammar"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Search programs as typed data. The size counter here IS the declared prior."

[dependencies]
